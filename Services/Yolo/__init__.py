import os
import shutil

# src = '/home/wqf/桌面/filelist.txt'
# det = '/home/wqf/桌面/train_data'
#
# with open(src) as f:
#     paths = [path.split('\n')[0] for path in f.readlines()]
# for path in paths:
#     print('copy file\t{}------------{}'.format(path, os.path.join(det, os.path.basename(path))))
#     shutil.copyfile(path, os.path.join(det, os.path.basename(path)))
